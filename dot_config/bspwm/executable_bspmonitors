#!/usr/bin/env bash
# This script was generated by bashly 1.1.0 (https://bashly.dannyb.co)
# Modifying it manually is not recommended

# :wrapper.bash3_bouncer
if [[ "${BASH_VERSINFO:-0}" -lt 4 ]]; then
  printf "bash version 4 or higher is required\n" >&2
  exit 1
fi

# :command.master_script

# :command.version_command
version_command() {
  echo "$version"
}

# :command.usage
bspmonitors_usage() {
  if [[ -n $long_usage ]]; then
    printf "bspmonitors - use pre-configured monitor layouts in ~/.scripts/settings.json and provide a nice cli to manage them\n"
    echo

  else
    printf "bspmonitors - use pre-configured monitor layouts in ~/.scripts/settings.json and provide a nice cli to manage them\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  bspmonitors [OPTIONS] COMMAND\n"
  printf "  bspmonitors [COMMAND] --help | -h\n"
  printf "  bspmonitors --version | -v\n"
  echo
  # :command.usage_commands
  printf "%s\n" "Commands:"
  printf "  %s   puts all monitors side to side\n" "auto  "
  printf "  %s   check if all monitors in a given layout are available. Prints available in success if not prints nothing\n" "check "
  printf "  %s   switch to a given layout\n" "layout"
  printf "  %s   get assigned name of an layout/monitor alias\n" "query "
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--list_monitors"
    printf "    list all configured monitors\n"
    echo

    # :flag.usage
    printf "  %s\n" "--list_layouts"
    printf "    list all configured layouts\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo
    printf "  %s\n" "--version, -v"
    printf "    Show version number\n"
    echo

  fi
}

# :command.usage
bspmonitors_list_usage() {
  if [[ -n $long_usage ]]; then
    printf "bspmonitors list\n"
    echo

  else
    printf "bspmonitors list\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  bspmonitors list [OPTIONS]\n"
  printf "  bspmonitors list --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--list_monitors"
    printf "    list all configured monitors\n"
    echo

    # :flag.usage
    printf "  %s\n" "--list_layouts"
    printf "    list all configured layouts\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
bspmonitors_auto_usage() {
  if [[ -n $long_usage ]]; then
    printf "bspmonitors auto - puts all monitors side to side\n"
    echo

  else
    printf "bspmonitors auto - puts all monitors side to side\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  bspmonitors auto\n"
  printf "  bspmonitors auto --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
bspmonitors_check_usage() {
  if [[ -n $long_usage ]]; then
    printf "bspmonitors check - check if all monitors in a given layout are available. Prints available in success if not prints nothing\n"
    echo

  else
    printf "bspmonitors check - check if all monitors in a given layout are available. Prints available in success if not prints nothing\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  bspmonitors check NAME\n"
  printf "  bspmonitors check --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_args
    printf "%s\n" "Arguments:"

    # :argument.usage
    printf "  %s\n" "NAME"
    printf "    layout name\n"
    echo

  fi
}

# :command.usage
bspmonitors_layout_usage() {
  if [[ -n $long_usage ]]; then
    printf "bspmonitors layout - switch to a given layout\n"
    echo

  else
    printf "bspmonitors layout - switch to a given layout\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  bspmonitors layout NAME\n"
  printf "  bspmonitors layout --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_args
    printf "%s\n" "Arguments:"

    # :argument.usage
    printf "  %s\n" "NAME"
    printf "    layout name\n"
    echo

  fi
}

# :command.usage
bspmonitors_query_usage() {
  if [[ -n $long_usage ]]; then
    printf "bspmonitors query - get assigned name of an layout/monitor alias\n"
    echo

  else
    printf "bspmonitors query - get assigned name of an layout/monitor alias\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  bspmonitors query [OPTIONS]\n"
  printf "  bspmonitors query --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--monitor QUERY"
    printf "    query monitor\n"
    echo

    # :flag.usage
    printf "  %s\n" "--layout QUERY"
    printf "    query layout\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.normalize_input
normalize_input() {
  local arg flags

  while [[ $# -gt 0 ]]; do
    arg="$1"
    if [[ $arg =~ ^(--[a-zA-Z0-9_\-]+)=(.+)$ ]]; then
      input+=("${BASH_REMATCH[1]}")
      input+=("${BASH_REMATCH[2]}")
    elif [[ $arg =~ ^(-[a-zA-Z0-9])=(.+)$ ]]; then
      input+=("${BASH_REMATCH[1]}")
      input+=("${BASH_REMATCH[2]}")
    elif [[ $arg =~ ^-([a-zA-Z0-9][a-zA-Z0-9]+)$ ]]; then
      flags="${BASH_REMATCH[1]}"
      for ((i = 0; i < ${#flags}; i++)); do
        input+=("-${flags:i:1}")
      done
    else
      input+=("$arg")
    fi

    shift
  done
}
# :command.inspect_args
inspect_args() {
  if ((${#args[@]})); then
    readarray -t sorted_keys < <(printf '%s\n' "${!args[@]}" | sort)
    echo args:
    for k in "${sorted_keys[@]}"; do echo "- \${args[$k]} = ${args[$k]}"; done
  else
    echo args: none
  fi

  if ((${#other_args[@]})); then
    echo
    echo other_args:
    echo "- \${other_args[*]} = ${other_args[*]}"
    for i in "${!other_args[@]}"; do
      echo "- \${other_args[$i]} = ${other_args[$i]}"
    done
  fi

  if ((${#deps[@]})); then
    readarray -t sorted_keys < <(printf '%s\n' "${!deps[@]}" | sort)
    echo
    echo deps:
    for k in "${sorted_keys[@]}"; do echo "- \${deps[$k]} = ${deps[$k]}"; done
  fi

}

# :command.command_functions
# :command.function
bspmonitors_list_command() {
  # src/list_command.sh

  if [ -z "${args[--list_monitors]}" ] && [ -z "${args[--list_layouts]}" ]; then
      bspmonitors --help
      exit
  fi

  if [ "${args[--list_monitors]}" = "1" ]; then
      archdots settings '.monitor.setup|map(.alias).[]'
  elif [ "${args[--list_layouts]}" = "1" ]; then
      archdots settings '.monitor.layouts|map(.name).[]'
  fi

}

# :command.function
bspmonitors_auto_command() {
  # src/auto_command.sh

  all_monitors="$(xrandr -q|grep '\bconnected'|awk '{ print $1 }')"
  readarray -t all_monitors <<< $all_monitors
  prev_item="${all_monitors[0]}"

  xrandr_command="xrandr --output $prev_item --auto --primary"
  bspc_command="bspc monitor $prev_item -d 1 2 3 4 5"
  id=5

  for item in ${all_monitors[@]}; do
      if [[ "$item" != "$prev_item" ]]; then
          xrandr_command="$xrandr_command --output $item --auto --right-of $prev_item"

          bspc_command="$bspc_command;bspc monitor $item -d $((id+1)) $((id+2)) $((id+3)) $((id+4)) $((id+5))"
          id=$((id+5))

          prev_item="$item"
      fi
  done

  eval $xrandr_command
  eval $bspc_command

}

# :command.function
bspmonitors_check_command() {
  # src/check_command.sh
  # echo "# this file is located in 'src/check_command.sh'"
  # echo "# code for 'bspmonitors check' goes here"
  # echo "# you can edit it freely and regenerate (it will not be overwritten)"
  # inspect_args

  layout="${args[name]}"
  layout_monitors="$(bspmonitors query --layout $layout|jq '.[0].layout[]|select(.enabled==true).monitor_alias' -r 2> /dev/null|xargs -i bspmonitors query --monitor {})"
  all_monitors="$(xrandr -q|grep '\bconnected'|awk '{ print $1 }')"

  for item in ${layout_monitors[@]}; do
      if [ -z "$(echo -n "$all_monitors"|grep "^$item$")" ]; then
          return
      fi
  done

  echo "available"

}

# :command.function
bspmonitors_layout_command() {
  # src/layout_command.sh
  # echo "# this file is located in 'src/layout_command.sh'"
  # echo "# code for 'bspmonitors layout' goes here"
  # echo "# you can edit it freely and regenerate (it will not be overwritten)"
  # inspect_args

  layout="$(archdots settings ".monitor.layouts|map(select(.name == \"${args[name]}\"))[0]"
  monitor_setup="$(archdots settings ".monitor.setup")"
  n_layout=$(echo "$layout"|jq -r '.layout|length')

  json ()
  {
      out="$(echo "$1"|jq -r "$2" 2> /dev/null)"
      if [ "$out" != "null" ]; then
          echo "$out"
      fi
  }

  output="xrandr "
  output_bspc=""
  for i in $(seq 0 $((n_layout-1))); do
      monitor_alias="$(json "$layout" ".layout[$i].monitor_alias")"
      primary="$(json "$layout" ".layout[$i].primary")"
      enabled="$(json "$layout" ".layout[$i].enabled")"
      x="$(json "$layout" ".layout[$i].x")"
      y="$(json "$layout" ".layout[$i].y")"
      bspwm_desktops="$(json "$layout" ".layout[$i].bspwm_desktops")"
      mirror="$(json "$layout" ".layout[$i].mirror")"
      width_override="$(json "$layout" ".layout[$i].width")"
      height_override="$(json "$layout" ".layout[$i].height")"

      monitor_config="$(json "$monitor_setup" "map(select(.alias == \"$monitor_alias\"))[0]")"

      width="$(json "$monitor_config" ".width")"
      height="$(json "$monitor_config" ".height")"
      monitor="$(json "$monitor_config" ".name")"

      output="$output --output $monitor"
      if [ "$enabled" = "true" ]; then

          [ "$primary" = "true" ] && output="$output --primary"

          if [ -n "$width_override" ] && [ -n "$height_override" ]; then
              output="$output --mode ${width_override}x${height_override}"
          elif [ -n "$width" ] && [ -n "$height" ]; then
              output="$output --mode ${width}x${height}"
          fi

          [ -n "$x" ] && [ -n "$y" ] && output="$output --pos ${x}x${y}"

          if [ -n "$mirror" ]; then
              mirror_name="$(json "$monitor_setup" "map(select(.alias == \"$mirror\"))[0].name")"
              # mirror_name="$(echo $monitor_setup|jq -r "map(select(.alias == \"$mirror\"))[0].name")"
              output="$output --same-as $mirror_name"
          fi

          [ -n "$bspwm_desktops" ] && output_bspc="$output_bspc; bspc monitor $monitor -d $bspwm_desktops"

      else
          output="$output --off"
      fi
  done

  eval "$output"
  eval "$(echo $output_bspc|sed 's/;//')"

}

# :command.function
bspmonitors_query_command() {
  # src/query_command.sh

  if [ -z "${args[--monitor]}" ] && [ -z "${args[--layout]}" ]; then
      bspmonitors query --help
      exit
  fi

  if [ -n "${args[--monitor]}" ]; then
      found="$(archdots settings '.monitor.setup|map(.alias).[]' |grep "^${args[--monitor]}$"|tail -n1)"
      archdots settings ".monitor.setup|map(select(.alias == \"$found\"))[0].name"

  elif [ -n "${args[--layout]}" ]; then
      found="$(archdots settings '.monitor.layouts|map(.name).[]' |grep "^${args[--layout]}$"|tail -n1)"
      archdots settings ".monitor.layouts|map(select(.name == \"$found\"))"

  fi

}

# :command.parse_requirements
parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --version | -v)
        version_command
        exit
        ;;

      --help | -h)
        long_usage=yes
        bspmonitors_usage
        exit
        ;;

      # :flag.case
      --list_monitors)

        # :flag.case_no_arg
        args['--list_monitors']=1
        shift
        ;;

      # :flag.case
      --list_layouts)

        # :flag.case_no_arg
        args['--list_layouts']=1
        shift
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action=${1:-}

  case $action in
    -*) ;;

    list)
      action="list"
      shift
      bspmonitors_list_parse_requirements "$@"
      shift $#
      ;;

    auto)
      action="auto"
      shift
      bspmonitors_auto_parse_requirements "$@"
      shift $#
      ;;

    check)
      action="check"
      shift
      bspmonitors_check_parse_requirements "$@"
      shift $#
      ;;

    layout)
      action="layout"
      shift
      bspmonitors_layout_parse_requirements "$@"
      shift $#
      ;;

    query)
      action="query"
      shift
      bspmonitors_query_parse_requirements "$@"
      shift $#
      ;;

    # :command.command_fallback
    *)
      action="list"
      bspmonitors_list_parse_requirements "$@"
      shift $#
      ;;

  esac

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
bspmonitors_list_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        bspmonitors_list_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="list"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --list_monitors)

        # :flag.case_no_arg
        args['--list_monitors']=1
        shift
        ;;

      # :flag.case
      --list_layouts)

        # :flag.case_no_arg
        args['--list_layouts']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
bspmonitors_auto_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        bspmonitors_auto_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="auto"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
bspmonitors_check_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        bspmonitors_check_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="check"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        if [[ -z ${args['name']+x} ]]; then

          args['name']=$1
          shift
        else
          printf "invalid argument: %s\n" "$key" >&2
          exit 1
        fi

        ;;

    esac
  done

  # :command.required_args_filter
  if [[ -z ${args['name']+x} ]]; then
    printf "missing required argument: NAME\nusage: bspmonitors check NAME\n" >&2
    exit 1
  fi

}

# :command.parse_requirements
bspmonitors_layout_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        bspmonitors_layout_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="layout"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        if [[ -z ${args['name']+x} ]]; then

          args['name']=$1
          shift
        else
          printf "invalid argument: %s\n" "$key" >&2
          exit 1
        fi

        ;;

    esac
  done

  # :command.required_args_filter
  if [[ -z ${args['name']+x} ]]; then
    printf "missing required argument: NAME\nusage: bspmonitors layout NAME\n" >&2
    exit 1
  fi

}

# :command.parse_requirements
bspmonitors_query_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        bspmonitors_query_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="query"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --monitor)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then

          args['--monitor']="$2"
          shift
          shift
        else
          printf "%s\n" "--monitor requires an argument: --monitor QUERY" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --layout)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then

          args['--layout']="$2"
          shift
          shift
        else
          printf "%s\n" "--layout requires an argument: --layout QUERY" >&2
          exit 1
        fi
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.initialize
initialize() {
  version="0.1.0"
  long_usage=''
  set -e

}

# :command.run
run() {
  declare -A args=()
  declare -A deps=()
  declare -a other_args=()
  declare -a input=()
  normalize_input "$@"
  parse_requirements "${input[@]}"

  case "$action" in
    "list") bspmonitors_list_command ;;
    "auto") bspmonitors_auto_command ;;
    "check") bspmonitors_check_command ;;
    "layout") bspmonitors_layout_command ;;
    "query") bspmonitors_query_command ;;
  esac
}

initialize
run "$@"
