#!/usr/bin/env bash
# This script was generated by bashly 1.1.0 (https://bashly.dannyb.co)
# Modifying it manually is not recommended

# :wrapper.bash3_bouncer
if [[ "${BASH_VERSINFO:-0}" -lt 4 ]]; then
  printf "bash version 4 or higher is required\n" >&2
  exit 1
fi

# :command.master_script

# :command.version_command
version_command() {
  echo "$version"
}

# :command.usage
archdots_usage() {
  if [[ -n $long_usage ]]; then
    printf "archdots - A tool for managing arch dotfiles and packages\n"
    echo

  else
    printf "archdots - A tool for managing arch dotfiles and packages\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  archdots COMMAND\n"
  printf "  archdots [COMMAND] --help | -h\n"
  printf "  archdots --version | -v\n"
  echo
  # :command.usage_commands
  printf "%s\n" "Commands:"
  printf "  %s   Run a setup script\n" "setup "
  printf "  %s   edit packages file\n" "edit  "
  printf "  %s   show diff since last dotfiles sync\n" "diff  "
  printf "  %s   Sync apps and dotfiles\n" "sync  "
  printf "  %s   go to chezmoi repo or pacdef groups folder location\n" "cd    "
  printf "  %s   review unmanaged packages\n" "review"
  printf "  %s   an alias for archdots git status\n" "status"
  printf "  %s   run git commands from chezmoi repo\n" "git   "
  printf "  %s   list packages (default) or tracked dotfiles\n" "list  "
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo
    printf "  %s\n" "--version, -v"
    printf "    Show version number\n"
    echo

  fi
}

# :command.usage
archdots_setup_usage() {
  if [[ -n $long_usage ]]; then
    printf "archdots setup - Run a setup script\n"
    echo

  else
    printf "archdots setup - Run a setup script\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  archdots setup COMMAND\n"
  printf "  archdots setup [COMMAND] --help | -h\n"
  echo
  # :command.usage_commands
  printf "%s\n" "Commands:"
  printf "  %s   \n" "all            "
  printf "  %s   \n" "git-credentials"
  printf "  %s   \n" "git-delta      "
  printf "  %s   \n" "nvim           "
  printf "  %s   \n" "evrouter       "
  printf "  %s   \n" "monitors       "
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
archdots_setup_all_usage() {
  if [[ -n $long_usage ]]; then
    printf "archdots setup all\n"
    echo

  else
    printf "archdots setup all\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  archdots setup all [OPTIONS]\n"
  printf "  archdots setup all --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--force"
    printf "    run even if setup is marked as ok\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
archdots_setup_git_credentials_usage() {
  if [[ -n $long_usage ]]; then
    printf "archdots setup git-credentials\n"
    echo

  else
    printf "archdots setup git-credentials\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  archdots setup git-credentials\n"
  printf "  archdots setup git-credentials --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
archdots_setup_git_delta_usage() {
  if [[ -n $long_usage ]]; then
    printf "archdots setup git-delta\n"
    echo

  else
    printf "archdots setup git-delta\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  archdots setup git-delta\n"
  printf "  archdots setup git-delta --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
archdots_setup_nvim_usage() {
  if [[ -n $long_usage ]]; then
    printf "archdots setup nvim\n"
    echo

  else
    printf "archdots setup nvim\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  archdots setup nvim\n"
  printf "  archdots setup nvim --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
archdots_setup_evrouter_usage() {
  if [[ -n $long_usage ]]; then
    printf "archdots setup evrouter\n"
    echo

  else
    printf "archdots setup evrouter\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  archdots setup evrouter\n"
  printf "  archdots setup evrouter --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
archdots_setup_monitors_usage() {
  if [[ -n $long_usage ]]; then
    printf "archdots setup monitors\n"
    echo

  else
    printf "archdots setup monitors\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  archdots setup monitors\n"
  printf "  archdots setup monitors --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
archdots_edit_usage() {
  if [[ -n $long_usage ]]; then
    printf "archdots edit - edit packages file\n"
    echo

  else
    printf "archdots edit - edit packages file\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  archdots edit [GROUP] [OPTIONS]\n"
  printf "  archdots edit --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--list"
    printf "    list all groups (package definition files)\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_args
    printf "%s\n" "Arguments:"

    # :argument.usage
    printf "  %s\n" "GROUP"
    printf "    specific group to edit\n"
    echo

  fi
}

# :command.usage
archdots_diff_usage() {
  if [[ -n $long_usage ]]; then
    printf "archdots diff - show diff since last dotfiles sync\n"
    echo

  else
    printf "archdots diff - show diff since last dotfiles sync\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  archdots diff\n"
  printf "  archdots diff --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
archdots_sync_usage() {
  if [[ -n $long_usage ]]; then
    printf "archdots sync - Sync apps and dotfiles\n"
    echo

  else
    printf "archdots sync - Sync apps and dotfiles\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  archdots sync COMMAND\n"
  printf "  archdots sync [COMMAND] --help | -h\n"
  echo
  # :command.usage_commands
  printf "%s\n" "Commands:"
  printf "  %s   run all setup scripts (default)\n" "all "
  printf "  %s   \n" "apps"
  printf "  %s   \n" "dots"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
archdots_sync_all_usage() {
  if [[ -n $long_usage ]]; then
    printf "archdots sync all - run all setup scripts\n"
    echo

  else
    printf "archdots sync all - run all setup scripts\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  archdots sync all\n"
  printf "  archdots sync all --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
archdots_sync_apps_usage() {
  if [[ -n $long_usage ]]; then
    printf "archdots sync apps\n"
    echo

  else
    printf "archdots sync apps\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  archdots sync apps [OPTIONS]\n"
  printf "  archdots sync apps --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--remote"
    printf "    only use remote changes\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
archdots_sync_dots_usage() {
  if [[ -n $long_usage ]]; then
    printf "archdots sync dots\n"
    echo

  else
    printf "archdots sync dots\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  archdots sync dots\n"
  printf "  archdots sync dots --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
archdots_cd_usage() {
  if [[ -n $long_usage ]]; then
    printf "archdots cd - go to chezmoi repo or pacdef groups folder location\n"
    echo

  else
    printf "archdots cd - go to chezmoi repo or pacdef groups folder location\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  archdots cd COMMAND\n"
  printf "  archdots cd [COMMAND] --help | -h\n"
  echo
  # :command.usage_commands
  printf "%s\n" "Commands:"
  printf "  %s   \n" "dots"
  printf "  %s   \n" "apps"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
archdots_cd_dots_usage() {
  if [[ -n $long_usage ]]; then
    printf "archdots cd dots\n"
    echo

  else
    printf "archdots cd dots\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  archdots cd dots\n"
  printf "  archdots cd dots --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
archdots_cd_apps_usage() {
  if [[ -n $long_usage ]]; then
    printf "archdots cd apps\n"
    echo

  else
    printf "archdots cd apps\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  archdots cd apps\n"
  printf "  archdots cd apps --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
archdots_review_usage() {
  if [[ -n $long_usage ]]; then
    printf "archdots review - review unmanaged packages\n"
    echo

  else
    printf "archdots review - review unmanaged packages\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  archdots review\n"
  printf "  archdots review --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
archdots_status_usage() {
  if [[ -n $long_usage ]]; then
    printf "archdots status - an alias for archdots git status\n"
    echo

  else
    printf "archdots status - an alias for archdots git status\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  archdots status\n"
  printf "  archdots status --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
archdots_git_usage() {
  if [[ -n $long_usage ]]; then
    printf "archdots git - run git commands from chezmoi repo\n"
    echo

  else
    printf "archdots git - run git commands from chezmoi repo\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  archdots git [...]\n"
  printf "  archdots git --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
archdots_list_usage() {
  if [[ -n $long_usage ]]; then
    printf "archdots list - list packages (default) or tracked dotfiles\n"
    echo

  else
    printf "archdots list - list packages (default) or tracked dotfiles\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  archdots list [OPTIONS] COMMAND\n"
  printf "  archdots list [COMMAND] --help | -h\n"
  echo
  # :command.usage_commands
  printf "%s\n" "Commands:"
  printf "  %s   \n" "dots"
  printf "  %s    (default)\n" "apps"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--unmanaged"
    printf "    only show unmanaged packages\n"
    echo

    # :flag.usage
    printf "  %s\n" "--pending"
    printf "    only show pending packages\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
archdots_list_dots_usage() {
  if [[ -n $long_usage ]]; then
    printf "archdots list dots\n"
    echo

  else
    printf "archdots list dots\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  archdots list dots [OPTIONS]\n"
  printf "  archdots list dots --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--unmanaged"
    printf "    only show unmanaged packages\n"
    echo

    # :flag.usage
    printf "  %s\n" "--tree"
    printf "    display as a tree\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
archdots_list_apps_usage() {
  if [[ -n $long_usage ]]; then
    printf "archdots list apps\n"
    echo

  else
    printf "archdots list apps\n"
    echo

  fi

  printf "%s\n" "Usage:"
  printf "  archdots list apps [OPTIONS]\n"
  printf "  archdots list apps --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "%s\n" "Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--unmanaged"
    printf "    only show unmanaged packages\n"
    echo

    # :flag.usage
    printf "  %s\n" "--pending"
    printf "    only show pending packages\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.normalize_input
normalize_input() {
  local arg flags

  while [[ $# -gt 0 ]]; do
    arg="$1"
    if [[ $arg =~ ^(--[a-zA-Z0-9_\-]+)=(.+)$ ]]; then
      input+=("${BASH_REMATCH[1]}")
      input+=("${BASH_REMATCH[2]}")
    elif [[ $arg =~ ^(-[a-zA-Z0-9])=(.+)$ ]]; then
      input+=("${BASH_REMATCH[1]}")
      input+=("${BASH_REMATCH[2]}")
    elif [[ $arg =~ ^-([a-zA-Z0-9][a-zA-Z0-9]+)$ ]]; then
      flags="${BASH_REMATCH[1]}"
      for ((i = 0; i < ${#flags}; i++)); do
        input+=("-${flags:i:1}")
      done
    else
      input+=("$arg")
    fi

    shift
  done
}
# :command.inspect_args
inspect_args() {
  if ((${#args[@]})); then
    readarray -t sorted_keys < <(printf '%s\n' "${!args[@]}" | sort)
    echo args:
    for k in "${sorted_keys[@]}"; do echo "- \${args[$k]} = ${args[$k]}"; done
  else
    echo args: none
  fi

  if ((${#other_args[@]})); then
    echo
    echo other_args:
    echo "- \${other_args[*]} = ${other_args[*]}"
    for i in "${!other_args[@]}"; do
      echo "- \${other_args[$i]} = ${other_args[$i]}"
    done
  fi

  if ((${#deps[@]})); then
    readarray -t sorted_keys < <(printf '%s\n' "${!deps[@]}" | sort)
    echo
    echo deps:
    for k in "${sorted_keys[@]}"; do echo "- \${deps[$k]} = ${deps[$k]}"; done
  fi

}

# :command.command_functions

# :command.function
archdots_setup_all_command() {
  # src/setup_all_command.sh

  commands=(
  "git_credentials"
  "git_delta"
  "nvim"
  )

  for item in ${commands[@]}; do

  	if [[ "$($(dirname $0)/src/setup_${item}_command.sh check)" = "notok" ]] || [[ ${args[--force]} -eq 1 ]]; then

  		gum style --foreground 212 --padding "1 4" --border rounded "Setup $item"
  		source $(dirname $0)/src/setup_${item}_command.sh
  	fi
  done

}

# :command.function
archdots_setup_git_credentials_command() {
  # src/setup_git_credentials_command.sh
  #!/usr/bin/env bash

  useremail="$(git config --global --get user.email||echo '')"
  username="$(git config --global --get user.name||echo '')"

  if [ "$1" = "check" ]; then
  	if [[ -z "$useremail" ]] || [[ -z "$username" ]]; then
  		echo notok
  	fi
  	exit
  fi

  useremail=$(gum input --placeholder "Type user.email...")
  echo "user.email: $useremail"
  username=$(gum input --placeholder "Type user.name...")
  echo "user.name: $username"

  [ -n "$useremail" ]&&git config --global user.email "$useremail"||echo "Empty input. Did not change previous"
  [ -n "$username" ]&&git config --global user.name "$username"||echo "Empty input. Did not change previous"

}

# :command.function
archdots_setup_git_delta_command() {
  # src/setup_git_delta_command.sh
  #!/usr/bin/env bash

  if [ "$1" = "check" ]; then
  	if [[ -z "$(cat ~/.gitconfig|grep 'pager = delta')" ]]; then
  		echo notok
  	fi
  	exit
  fi

  if [[ -z "$(cat ~/.gitconfig|grep 'pager = delta')" ]]; then
  	echo Appending git delta configuration to $HOME/.gitconfig
  	cat "$(dirname $0)/public/gitdelta_snippet.txt" >> $HOME/.gitconfig

  else
  	echo Alredy configured
  fi

}

# :command.function
archdots_setup_nvim_command() {
  # src/setup_nvim_command.sh
  #!/usr/bin/env bash

  if [ "$1" = "check" ]; then
  	if [[ ! -d "$HOME/.config/nvim" ]]; then
  		echo notok
  	fi
  	exit
  fi

  if [[ ! -d "$HOME/.config/nvim" ]]; then
  	echo cloning nvim repo
  	git clone "git@github.com:AlanJs26/nvim_config.git"
  else
  	echo Alredy configured
  fi

}

# :command.function
archdots_setup_evrouter_command() {
  # src/setup_evrouter_command.sh
  #!/usr/bin/env bash

  if [ "$1" = "check" ]; then

  	if [ ! -d "$(dirname $0)/public/evrouter/pkg" ]; then
  		echo notok
  	fi
  	exit
  fi

  if [ ! -d "$(dirname $0)/public/evrouter/pkg" ]; then
  	(cd "$(dirname $0)/public/evrouter" && makepkg --install)
  else
  	echo Alredy configured
  fi

}

# :command.function
archdots_setup_monitors_command() {
  # src/setup_monitors_command.sh
  # echo "# this file is located in 'src/setup_monitors_command.sh'"
  # echo "# code for 'archdots setup monitors' goes here"
  # echo "# you can edit it freely and regenerate (it will not be overwritten)"
  # inspect_args

  apps=$HOME/.config/bspwm/apps.json

  available_monitors=($(xrandr -q|rg connected|awk '{ print $1 }'))

  primary_monitor="$(gum choose --header="Primary Monitor $(jq .monitors.primary $apps)" "${available_monitors[@]}")"
  secundary_monitor="$(gum choose --header="Secundary Monitor $(jq .monitors.secundary $apps)" "${available_monitors[@]}")"
  tv_monitor="$(gum choose --header="TV $(jq .monitors.tv $apps)" "${available_monitors[@]}")"

  result="$(cat $apps|jq\
  	--arg primary $primary_monitor\
  	--arg secundary $secundary_monitor\
  	--arg tv $tv_monitor\
  	'.monitors.primary = $primary | .monitors.secundary = $secundary | .monitors.tv = $tv')"

  echo "$result"|jq '.' > $apps

  # for item in ${available_monitors[@]}; do
  # 	echo $item
  # done

}

# :command.function
archdots_edit_command() {
  # src/edit_command.sh

  group="${args[group]}"

  if [[ ${args[--list]} -eq 1 ]]; then
  	pacdef group list
  elif [[ -n $group ]]; then
  	if [[ -n "$(pacdef group list|grep -w $group)" ]];then
  		cd "$HOME/.config/pacdef/groups"
  		$EDITOR "$HOME/.config/pacdef/groups/${args[group]}"
  	else
  		gum style --foreground="1" "invalid group: '$group'"
  		echo -e "\nvalid groups:"
  		pacdef group list

  	fi
  else
  	cd "$HOME/.config/pacdef/groups"
  	$EDITOR "$HOME/.config/pacdef/groups"
  fi

}

# :command.function
archdots_diff_command() {
  # src/diff_command.sh
  chezmoi diff --pager 'bat'

}

# :command.function
archdots_sync_all_command() {
  # src/sync_all_command.sh
  # echo "# this file is located in 'src/sync_all_command.sh'"
  # echo "# code for 'archdots sync all' goes here"
  # echo "# you can edit it freely and regenerate (it will not be overwritten)"
  # inspect_args

  [ -f "$(which gum)" ]&&gum style --foreground 212 --padding "1 4" --border rounded "Syncing Apps"
  source $(dirname $0)/src/sync_apps_command.sh

  [ -f "$(which gum)" ]&&gum style --foreground 212 --padding "1 4" --border rounded "Syncing Dots"
  source $(dirname $0)/src/sync_dots_command.sh

}

# :command.function
archdots_sync_apps_command() {
  # src/sync_apps_command.sh
  pacdef package sync 2> /dev/null

  unmanaged=$(echo "$(pacdef package unmanaged 2> /dev/null)"|rg -vU '^(WARNING|\[|#|\n)'|wc -l)

  if [ $unmanaged -gt 0 ]; then
  	echo -e "you have $unmanaged unmanaged packages\nRun 'archdots list --unmanaged' to view them or 'archdots review' to decide what to do with each one"
  fi

}

# :command.function
archdots_sync_dots_command() {
  # src/sync_dots_command.sh

  cd ~/.local/share/chezmoi

  echo $(pwd)
  if [ -f "$(which gum)" ]; then
  	git add .
  	if [[ ${args[--remote]} -ne 1 ]]; then
  		chezmoi re-add
  	fi
  fi

  chezmoi update||echo Failed to update

  git diff --cached --stat

  if [[ $(git diff --cached --numstat|wc -l) -gt 0 ]]; then
  	if $(gum confirm "push changes"); then
  		randomstr="$(openssl rand -base64 12)"
  		message="$randomstr $(date +'%d-%m-%Y')"

  		if !($(gum confirm "use default message: '$message'?")); then
  			message="$(gum input --placeholder 'Type a new message...')"
  		fi
  		git commit -m "$message"
  		git push
  	fi

  fi

}

# :command.function
archdots_cd_dots_command() {
  # src/cd_dots_command.sh
  #!/usr/bin/env sh

  if [[ -n $CHEZMOI_NESTED ]]; then
  	echo cannot create another nested shell. Please press Ctrl+D and run again
  	exit
  fi

  echo you are inside a nested shell
  export CHEZMOI_NESTED="y"
  cd $HOME/.local/share/chezmoi
  $SHELL

}

# :command.function
archdots_cd_apps_command() {
  # src/cd_apps_command.sh
  #!/usr/bin/env sh

  if [[ -n $PACDEF_NESTED ]]; then
  	echo cannot create another nested shell. Please press Ctrl+D and run again
  	exit
  fi

  echo you are inside a nested shell
  export PACDEF_NESTED="y"
  cd $HOME/.config/pacdef
  $SHELL

}

# :command.function
archdots_review_command() {
  # src/review_command.sh
  # echo "# this file is located in 'src/review_command.sh'"
  # echo "# code for 'archdots review' goes here"
  # echo "# you can edit it freely and regenerate (it will not be overwritten)"
  # inspect_args

  pacdef package review

}

# :command.function
archdots_status_command() {
  # src/status_command.sh
  # echo "# this file is located in 'src/status_command.sh'"
  # echo "# code for 'archdots status' goes here"
  # echo "# you can edit it freely and regenerate (it will not be overwritten)"
  # inspect_args

  "$(dirname $0)/archdots" git status

}

# :command.function
archdots_git_command() {
  # src/git_command.sh
  # echo "# this file is located in 'src/git_command.sh'"
  # echo "# code for 'archdots git' goes here"
  # echo "# you can edit it freely and regenerate (it will not be overwritten)"
  # inspect_args

  cd ~/.local/share/chezmoi
  git ${other_args[@]}

}

# :command.function
archdots_list_dots_command() {
  # src/list_dots_command.sh

  if [[ ${args[--unmanaged]} -eq 1 ]]; then
  	if [[ ${args[--tree]} -eq 1 ]]; then
  		tree --fromfile <(chezmoi unmanaged)
  	else
  		chezmoi unmanaged
  	fi
  else
  	if [[ ${args[--tree]} -eq 1 ]]; then
  		tree --fromfile <(chezmoi managed)
  	else
  		chezmoi managed
  	fi
  fi

}

# :command.function
archdots_list_apps_command() {
  # src/list_apps_command.sh

  if [[ ${args[--unmanaged]} -eq 1 ]]; then
  	pacdef package unmanaged 2> /dev/null
  elif [[ ${args[--pending]} -eq 1 ]]; then
  	# Isso não funciona para pacotes que não sejam arch

  	all_apps="$(cat ~/.config/pacdef/groups/*|rg -v '^(WARNING|\[|#)'|sort -u)"
  	installed_apps="$(yay -Q|awk '{ print $1 }'|sort -u)"

  	result=($(comm -13 <(echo "${installed_apps[@]}") <(echo "${all_apps[@]}")))
  	echo $result
  else
  	pacdef package search '' 2> /dev/null
  fi

}

# :command.parse_requirements
parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --version | -v)
        version_command
        exit
        ;;

      --help | -h)
        long_usage=yes
        archdots_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action=${1:-}

  case $action in
    -*) ;;

    setup)
      action="setup"
      shift
      archdots_setup_parse_requirements "$@"
      shift $#
      ;;

    edit)
      action="edit"
      shift
      archdots_edit_parse_requirements "$@"
      shift $#
      ;;

    diff)
      action="diff"
      shift
      archdots_diff_parse_requirements "$@"
      shift $#
      ;;

    sync)
      action="sync"
      shift
      archdots_sync_parse_requirements "$@"
      shift $#
      ;;

    cd)
      action="cd"
      shift
      archdots_cd_parse_requirements "$@"
      shift $#
      ;;

    review)
      action="review"
      shift
      archdots_review_parse_requirements "$@"
      shift $#
      ;;

    status)
      action="status"
      shift
      archdots_status_parse_requirements "$@"
      shift $#
      ;;

    git)
      action="git"
      shift
      archdots_git_parse_requirements "$@"
      shift $#
      ;;

    list)
      action="list"
      shift
      archdots_list_parse_requirements "$@"
      shift $#
      ;;

    # :command.command_fallback
    "")
      archdots_usage >&2
      exit 1
      ;;

    *)
      printf "invalid command: %s\n" "$action" >&2
      exit 1
      ;;

  esac

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
archdots_setup_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        archdots_setup_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action=${1:-}

  case $action in
    -*) ;;

    all)
      action="all"
      shift
      archdots_setup_all_parse_requirements "$@"
      shift $#
      ;;

    git-credentials)
      action="git-credentials"
      shift
      archdots_setup_git_credentials_parse_requirements "$@"
      shift $#
      ;;

    git-delta)
      action="git-delta"
      shift
      archdots_setup_git_delta_parse_requirements "$@"
      shift $#
      ;;

    nvim)
      action="nvim"
      shift
      archdots_setup_nvim_parse_requirements "$@"
      shift $#
      ;;

    evrouter)
      action="evrouter"
      shift
      archdots_setup_evrouter_parse_requirements "$@"
      shift $#
      ;;

    monitors)
      action="monitors"
      shift
      archdots_setup_monitors_parse_requirements "$@"
      shift $#
      ;;

    # :command.command_fallback
    "")
      archdots_setup_usage >&2
      exit 1
      ;;

    *)
      printf "invalid command: %s\n" "$action" >&2
      exit 1
      ;;

  esac

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
archdots_setup_all_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        archdots_setup_all_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="setup all"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --force)

        # :flag.case_no_arg
        args['--force']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
archdots_setup_git_credentials_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        archdots_setup_git_credentials_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="setup git-credentials"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
archdots_setup_git_delta_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        archdots_setup_git_delta_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="setup git-delta"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
archdots_setup_nvim_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        archdots_setup_nvim_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="setup nvim"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
archdots_setup_evrouter_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        archdots_setup_evrouter_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="setup evrouter"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
archdots_setup_monitors_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        archdots_setup_monitors_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="setup monitors"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
archdots_edit_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        archdots_edit_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="edit"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --list)

        # :flag.case_no_arg
        args['--list']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        if [[ -z ${args['group']+x} ]]; then

          args['group']=$1
          shift
        else
          printf "invalid argument: %s\n" "$key" >&2
          exit 1
        fi

        ;;

    esac
  done

}

# :command.parse_requirements
archdots_diff_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        archdots_diff_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="diff"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
archdots_sync_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        archdots_sync_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action=${1:-}

  case $action in
    -*) ;;

    all)
      action="all"
      shift
      archdots_sync_all_parse_requirements "$@"
      shift $#
      ;;

    apps)
      action="apps"
      shift
      archdots_sync_apps_parse_requirements "$@"
      shift $#
      ;;

    dots)
      action="dots"
      shift
      archdots_sync_dots_parse_requirements "$@"
      shift $#
      ;;

    # :command.command_fallback
    *)
      action="all"
      archdots_sync_all_parse_requirements "$@"
      shift $#
      ;;

  esac

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
archdots_sync_all_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        archdots_sync_all_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="sync all"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
archdots_sync_apps_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        archdots_sync_apps_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="sync apps"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --remote)

        # :flag.case_no_arg
        args['--remote']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
archdots_sync_dots_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        archdots_sync_dots_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="sync dots"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
archdots_cd_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        archdots_cd_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action=${1:-}

  case $action in
    -*) ;;

    dots)
      action="dots"
      shift
      archdots_cd_dots_parse_requirements "$@"
      shift $#
      ;;

    apps)
      action="apps"
      shift
      archdots_cd_apps_parse_requirements "$@"
      shift $#
      ;;

    # :command.command_fallback
    "")
      archdots_cd_usage >&2
      exit 1
      ;;

    *)
      printf "invalid command: %s\n" "$action" >&2
      exit 1
      ;;

  esac

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
archdots_cd_dots_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        archdots_cd_dots_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="cd dots"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
archdots_cd_apps_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        archdots_cd_apps_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="cd apps"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
archdots_review_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        archdots_review_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="review"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
archdots_status_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        archdots_status_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="status"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
archdots_git_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        archdots_git_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="git"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      --)
        shift
        other_args+=("$@")
        break
        ;;

      -?*)
        other_args+=("$1")
        shift
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_catch_all
        other_args+=("$1")
        shift

        ;;

    esac
  done

}

# :command.parse_requirements
archdots_list_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        archdots_list_usage
        exit
        ;;

      # :flag.case
      --unmanaged)

        # :flag.case_no_arg
        args['--unmanaged']=1
        shift
        ;;

      # :flag.case
      --pending)

        # :flag.case_no_arg
        args['--pending']=1
        shift
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action=${1:-}

  case $action in
    -*) ;;

    dots)
      action="dots"
      shift
      archdots_list_dots_parse_requirements "$@"
      shift $#
      ;;

    apps)
      action="apps"
      shift
      archdots_list_apps_parse_requirements "$@"
      shift $#
      ;;

    # :command.command_fallback
    *)
      action="apps"
      archdots_list_apps_parse_requirements "$@"
      shift $#
      ;;

  esac

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
archdots_list_dots_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        archdots_list_dots_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="list dots"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --unmanaged)

        # :flag.case_no_arg
        args['--unmanaged']=1
        shift
        ;;

      # :flag.case
      --tree)

        # :flag.case_no_arg
        args['--tree']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
archdots_list_apps_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    case "${1:-}" in
      --help | -h)
        long_usage=yes
        archdots_list_apps_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="list apps"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      # :flag.case
      --unmanaged)

        # :flag.case_no_arg
        args['--unmanaged']=1
        shift
        ;;

      # :flag.case
      --pending)

        # :flag.case_no_arg
        args['--pending']=1
        shift
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.initialize
initialize() {
  version="0.1.0"
  long_usage=''
  set -e

}

# :command.run
run() {
  declare -A args=()
  declare -A deps=()
  declare -a other_args=()
  declare -a input=()
  normalize_input "$@"
  parse_requirements "${input[@]}"

  case "$action" in
    "setup") archdots_setup_command ;;
    "setup all") archdots_setup_all_command ;;
    "setup git-credentials") archdots_setup_git_credentials_command ;;
    "setup git-delta") archdots_setup_git_delta_command ;;
    "setup nvim") archdots_setup_nvim_command ;;
    "setup evrouter") archdots_setup_evrouter_command ;;
    "setup monitors") archdots_setup_monitors_command ;;
    "edit") archdots_edit_command ;;
    "diff") archdots_diff_command ;;
    "sync") archdots_sync_command ;;
    "sync all") archdots_sync_all_command ;;
    "sync apps") archdots_sync_apps_command ;;
    "sync dots") archdots_sync_dots_command ;;
    "cd") archdots_cd_command ;;
    "cd dots") archdots_cd_dots_command ;;
    "cd apps") archdots_cd_apps_command ;;
    "review") archdots_review_command ;;
    "status") archdots_status_command ;;
    "git") archdots_git_command ;;
    "list") archdots_list_command ;;
    "list dots") archdots_list_dots_command ;;
    "list apps") archdots_list_apps_command ;;
  esac
}

initialize
run "$@"
